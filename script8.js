const canvas = document.getElementById("myCanvas");
      const ctx = canvas.getContext("2d");
    
      ctx.fillStyle = ("blue"); // Mengatur warna isi menjadi merah
      ctx.fillRect(10, 10, 80, 50); // Menggambar persegi panjang